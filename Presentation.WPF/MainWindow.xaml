<Window x:Class="Presentation.WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="720" Width="1280" WindowStartupLocation="CenterScreen">

    <Grid Background="{StaticResource Background}">
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="auto"/>
        </Grid.ColumnDefinitions>
        <Border Grid.ColumnSpan="3" BorderThickness="0,0,0,1" BorderBrush="Gray">
            <ContentControl Content="{Binding GameStatusViewModel}"/>
        </Border>
        <ContentControl Content="{Binding PlayersViewModel}" Grid.Row="2"/>
        <ContentControl Grid.Row="2" Grid.Column="1" Margin="10" Content="{Binding BoardViewModel}"/>
        <Border Grid.Column="2" Grid.Row="2" Width="40"/>
    </Grid>
    
</Window>
