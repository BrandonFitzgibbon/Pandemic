<Window x:Class="Presentation.WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="900" Width="1600" WindowStartupLocation="CenterScreen">

    <Grid Background="{StaticResource Background}">
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Border Grid.Row="0" Grid.ColumnSpan="3" BorderThickness="0,0,0,1" BorderBrush="Gray">
            <ContentControl Content="{Binding GameStatusViewModel}"/>
        </Border>
        <Border Grid.Row="1" Grid.RowSpan="2" BorderThickness="0,0,1,0" BorderBrush="Gray">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <ContentControl Content="{Binding PlayersViewModel}"/>
                <ContentControl Grid.Row="1" Content="{Binding ActionsViewModel}"/>
            </Grid>
        </Border>
        <Grid Grid.Row="1" Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="250"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <ContentControl Grid.Row="0" Grid.Column="0" Grid.RowSpan="2" Margin="10" Content="{Binding BoardViewModel}"/>
            <Border Grid.Row="0" Grid.Column="1" Grid.RowSpan="2" Width="250" BorderThickness="1,0,0,0" BorderBrush="Gray">
                <ContentControl Content="{Binding DeckViewModel}"/>
            </Border>
            <Border Grid.Row="1" Grid.Column="0">
                <ContentControl  Content="{Binding HandViewModel}"/>
            </Border>
        </Grid>
    </Grid>
    
</Window>
