<UserControl x:Class="Presentation.WPF.Views.BoardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:c="clr-namespace:Presentation.WPF.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="720" d:DesignWidth="1280">
    
    <UserControl.Resources>
        <ImageBrush x:Key="worldmap" ImageSource="pack://application:,,,/Controls/worldmap.bmp"/>
        <c:DiseaseToColorConverter x:Key="DiseaseToColorConverter"/>
    </UserControl.Resources>
    
    <Grid>
        <ListView ItemsSource="{Binding Cities}" Background="Transparent" BorderBrush="Transparent" ScrollViewer.VerticalScrollBarVisibility="Hidden">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock DataContext="{Binding}" Foreground="{Binding Disease, Converter={StaticResource DiseaseToColorConverter}}">
                            <TextBlock.Text>
                                <MultiBinding StringFormat="{}{0}, {1}">
                                    <Binding Path="Name"/>
                                    <Binding Path="Country"/>
                                </MultiBinding>
                            </TextBlock.Text>
                        </TextBlock>
                        <StackPanel Grid.Row="2" Orientation="Vertical">
                            <HeaderedItemsControl Header="Connections" Margin="5" Foreground="White" ItemsSource="{Binding Connections}">
                                <HeaderedItemsControl.Template>
                                    <ControlTemplate TargetType="HeaderedItemsControl">
                                        <Border>
                                            <Grid>
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="auto" />
                                                    <RowDefinition Height="auto" />
                                                    <RowDefinition />
                                                </Grid.RowDefinitions>
                                                <ContentPresenter ContentSource="Header" />
                                                <Separator Grid.Row="1" />
                                                <ItemsPresenter Grid.Row="2" />
                                            </Grid>
                                        </Border>
                                    </ControlTemplate>
                                </HeaderedItemsControl.Template>
                                <ItemsControl.ItemContainerStyle>
                                    <Style TargetType="ContentPresenter">
                                        <Setter Property="Margin" Value="0,0,5,0"/>
                                    </Style>
                                </ItemsControl.ItemContainerStyle>
                            </HeaderedItemsControl>
                            <HeaderedItemsControl Header="Counters" Margin="5" Foreground="White" ItemsSource="{Binding Counters}">
                                <HeaderedItemsControl.Template>
                                    <ControlTemplate TargetType="HeaderedItemsControl">
                                        <Border>
                                            <Grid>
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="auto" />
                                                    <RowDefinition Height="auto" />
                                                    <RowDefinition />
                                                </Grid.RowDefinitions>
                                                <ContentPresenter ContentSource="Header" />
                                                <Separator Grid.Row="1" />
                                                <ItemsPresenter Grid.Row="2" />
                                            </Grid>
                                        </Border>
                                    </ControlTemplate>
                                </HeaderedItemsControl.Template>
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <UniformGrid Rows="2" Columns="2"/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                            </HeaderedItemsControl>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Rows="8" Columns="6"/>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="VerticalContentAlignment" Value="Top"/>
                </Style>
            </ListView.ItemContainerStyle>
        </ListView>
    </Grid>
    
</UserControl>
