<UserControl x:Class="Presentation.WPF.Views.GameStatusView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:e="clr-namespace:Engine.Implementations;assembly=Engine"
             xmlns:c="clr-namespace:Presentation.WPF.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="30" d:DesignWidth="300">
    
    <UserControl.Resources>
        
        <c:DiseaseToIconConverter x:Key="DiseaseToIconConverter"/>
        <c:DiseaseStatusToIconConverter x:Key="DiseaseStatusToIconConverter"/>
        
        <DataTemplate DataType="{x:Type e:DiseaseCounter}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Viewbox Grid.Column="0" Margin="3,5,0,5" HorizontalAlignment="Center">
                    <ContentControl Content="{Binding Disease, Converter={StaticResource DiseaseToIconConverter}}"/>
                </Viewbox>
                <TextBlock Grid.Column="1" Margin="5,0,5,0" Text="{Binding Count}" FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                <Viewbox Grid.Column="2" Margin="0,3,3,3" HorizontalAlignment="Center">
                    <ContentControl>
                        <ContentControl.Style>
                            <Style TargetType="ContentControl">
                                <Setter Property="Content" Value="{Binding Disease, Converter={StaticResource DiseaseStatusToIconConverter}}"/>
                                <Setter Property="Visibility" Value="Hidden"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Disease.IsCured}" Value="True">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding Disease.IsEradicated}" Value="True">
                                        <Setter Property="Visibility" Value="Visible"/>
                                        <Setter Property="Content" Value="{Binding Disease, Converter={StaticResource DiseaseStatusToIconConverter}}"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </ContentControl.Style>
                    </ContentControl>
                </Viewbox>
            </Grid>
        </DataTemplate>
        
    </UserControl.Resources>
    
    <Grid TextElement.Foreground="White">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="auto"/>
        </Grid.ColumnDefinitions>
        <Viewbox Grid.Column="0" Margin="10,3,3,3">
            <UniformGrid Columns="2">
                <ContentControl Content="{StaticResource biohazard}" ToolTip="Outbreaks"/>
                <TextBlock Text="{Binding OutbreakCounter.Count}" FontSize="72" HorizontalAlignment="Center" VerticalAlignment="Center" ToolTip="Outbreaks"/>
            </UniformGrid>
        </Viewbox>
        <Separator Grid.Column="1" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
            <Separator.LayoutTransform>
                <RotateTransform Angle="90"/>
            </Separator.LayoutTransform>
        </Separator>
        <ContentControl Grid.Column="2" Content="{Binding YellowCounter}"/>
        <Separator Grid.Column="3" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
            <Separator.LayoutTransform>
                <RotateTransform Angle="90"/>
            </Separator.LayoutTransform>
        </Separator>
        <ContentControl Grid.Column="4" Content="{Binding RedCounter}"/>
        <Separator Grid.Column="5" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
            <Separator.LayoutTransform>
                <RotateTransform Angle="90"/>
            </Separator.LayoutTransform>
        </Separator>
        <ContentControl Grid.Column="6" Content="{Binding BlueCounter}"/>
        <Separator Grid.Column="7" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
            <Separator.LayoutTransform>
                <RotateTransform Angle="90"/>
            </Separator.LayoutTransform>
        </Separator>
        <ContentControl Grid.Column="8" Content="{Binding BlackCounter}"/>
        <Separator Grid.Column="9" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
            <Separator.LayoutTransform>
                <RotateTransform Angle="90"/>
            </Separator.LayoutTransform>
        </Separator>
        <Viewbox Grid.Column="10" Margin="3,3,10,3">
            <Viewbox.Resources>
                <Style x:Key="InfectionEllipse" TargetType="Ellipse">
                    <Setter Property="Height" Value="50"/>
                    <Setter Property="Width" Value="50"/>
                    <Setter Property="Fill" Value="Green"/>
                </Style>
            </Viewbox.Resources>
            <StackPanel Orientation="Horizontal">
                <ContentControl Content="{StaticResource gasmask}"/>
                <StackPanel Margin="20,0,0,0" VerticalAlignment="Center">
                    <Ellipse>
                        <Ellipse.Style>
                            <Style TargetType="Ellipse" BasedOn="{StaticResource InfectionEllipse}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding InfectionCounterOne}" Value="True">
                                        <Setter Property="Fill" Value="Red"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Ellipse.Style>
                    </Ellipse>
                    <TextBlock Text="2" FontSize="52" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </StackPanel>
                <StackPanel Margin="20,0,0,0" VerticalAlignment="Center">
                    <Ellipse>
                        <Ellipse.Style>
                            <Style TargetType="Ellipse" BasedOn="{StaticResource InfectionEllipse}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding InfectionCounterTwo}" Value="True">
                                        <Setter Property="Fill" Value="Red"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Ellipse.Style>
                    </Ellipse>
                    <TextBlock Text="2" FontSize="52" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </StackPanel>
                <StackPanel Margin="20,0,0,0" VerticalAlignment="Center">
                    <Ellipse>
                        <Ellipse.Style>
                            <Style TargetType="Ellipse" BasedOn="{StaticResource InfectionEllipse}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding InfectionCounterThree}" Value="True">
                                        <Setter Property="Fill" Value="Red"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Ellipse.Style>
                    </Ellipse>
                    <TextBlock Text="2" FontSize="52" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </StackPanel>
                <StackPanel Margin="20,0,0,0" VerticalAlignment="Center">
                    <Ellipse>
                        <Ellipse.Style>
                            <Style TargetType="Ellipse" BasedOn="{StaticResource InfectionEllipse}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding InfectionCounterFour}" Value="True">
                                        <Setter Property="Fill" Value="Red"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Ellipse.Style>
                    </Ellipse>
                    <TextBlock Text="3" FontSize="52" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </StackPanel>
                <StackPanel Margin="20,0,0,0" VerticalAlignment="Center">
                    <Ellipse>
                        <Ellipse.Style>
                            <Style TargetType="Ellipse" BasedOn="{StaticResource InfectionEllipse}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding InfectionCounterFive}" Value="True">
                                        <Setter Property="Fill" Value="Red"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Ellipse.Style>
                    </Ellipse>
                    <TextBlock Text="3" FontSize="52" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </StackPanel>
                <StackPanel Margin="20,0,0,0" VerticalAlignment="Center">
                    <Ellipse>
                        <Ellipse.Style>
                            <Style TargetType="Ellipse" BasedOn="{StaticResource InfectionEllipse}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding InfectionCounterSix}" Value="True">
                                        <Setter Property="Fill" Value="Red"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Ellipse.Style>
                    </Ellipse>
                    <TextBlock Text="4" FontSize="52" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </StackPanel>
                <StackPanel Margin="20,0,0,0" VerticalAlignment="Center">
                    <Ellipse>
                        <Ellipse.Style>
                            <Style TargetType="Ellipse" BasedOn="{StaticResource InfectionEllipse}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding InfectionCounterSeven}" Value="True">
                                        <Setter Property="Fill" Value="Red"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Ellipse.Style>
                    </Ellipse>
                    <TextBlock Text="4" FontSize="52" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </StackPanel>
            </StackPanel>
        </Viewbox>
    </Grid>
    
</UserControl>
