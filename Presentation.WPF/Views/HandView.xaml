<UserControl x:Class="Presentation.WPF.Views.HandView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:e="clr-namespace:Engine.Implementations;assembly=Engine"
             xmlns:c="clr-namespace:Presentation.WPF.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" BorderBrush="Transparent">
    
    <UserControl.Resources>
        
        <c:DiseaseToColorConverter x:Key="DiseaseToColorConverter"/>
        <c:DiseaseToFontConverter x:Key="DiseaseToFontConverter"/>

    </UserControl.Resources>
    
    <Grid>
        <ListView ItemsSource="{Binding Cards}" Height="250" BorderBrush="Transparent" Background="Transparent">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ContentControl x:Name="Content">
                        <Grid TextElement.Foreground="{Binding City.Disease, Converter={StaticResource DiseaseToFontConverter}}">
                            <StackPanel VerticalAlignment="Top" Background="{Binding City.Disease, Converter={StaticResource DiseaseToColorConverter}}">
                                <TextBlock VerticalAlignment="Top" Text="{Binding City.Name}" TextWrapping="Wrap" FontSize="16" Margin="3"/>
                            </StackPanel>
                        </Grid>
                    </ContentControl>
                    <DataTemplate.Triggers>
                        <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=ListViewItem}, Path=IsSelected}" Value="True">
                            <Setter TargetName="Content" Property="Content">
                                <Setter.Value>
                                    <Grid TextElement.Foreground="{Binding City.Disease, Converter={StaticResource DiseaseToFontConverter}}">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="*"/>
                                            <RowDefinition Height="*"/>
                                        </Grid.RowDefinitions>
                                        <StackPanel VerticalAlignment="Top" Background="{Binding City.Disease, Converter={StaticResource DiseaseToColorConverter}}">
                                            <TextBlock VerticalAlignment="Top" Text="{Binding City.Name}" FontSize="16" Margin="3"/>
                                            <TextBlock VerticalAlignment="Bottom" Text="{Binding City.Country}" FontSize="12" FontWeight="SemiBold" Margin="3"/>
                                        </StackPanel>
                                        <StackPanel Grid.Row="1" VerticalAlignment="Bottom" Background="{Binding City.Disease, Converter={StaticResource DiseaseToColorConverter}}">
                                            <TextBlock VerticalAlignment="Top" Text="Population" FontSize="12" Margin="3"/>
                                            <TextBlock VerticalAlignment="Bottom" Text="{Binding City.Population, StringFormat={}{0:###,###,###}}" FontSize="12" Margin="3"/>
                                        </StackPanel>
                                    </Grid>
                                </Setter.Value>
                            </Setter>
                        </DataTrigger>
                    </DataTemplate.Triggers>
                </DataTemplate>
            </ListView.ItemTemplate>
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="Margin" Value="5"/>
                    <Setter Property="HorizontalContentAlignment" Value="Center"/>
                    <Setter Property="BorderBrush" Value="{Binding City.Disease, Converter={StaticResource DiseaseToColorConverter}}"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListViewItem">
                                <Border x:Name="Border" Height="150" Width="117" Background="Transparent" BorderBrush="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=BorderBrush}" BorderThickness="3" CornerRadius="3">
                                    <Grid>
                                        <Border CornerRadius="3">
                                            <Border.Background>
                                                <LinearGradientBrush Opacity="1" StartPoint="0,0" EndPoint="10,10" MappingMode="Absolute" SpreadMethod="Repeat">
                                                    <GradientStop Offset="0" Color="White"/>
                                                    <GradientStop Offset="0.5" Color="White"/>
                                                    <GradientStop Offset="0.5" Color="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=BorderBrush.Color}"/>
                                                    <GradientStop Offset="1" Color="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=BorderBrush.Color}"/>
                                                </LinearGradientBrush>
                                            </Border.Background>
                                        </Border>
                                        <ContentPresenter Margin="5"/>
                                    </Grid>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsSelected" Value="True">  
                                        <Setter TargetName="Border" Property="Height" Value="225"/>
                                        <Setter TargetName="Border" Property="Width" Value="175.5"/>  
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListView.ItemContainerStyle>
        </ListView>        
    </Grid>
    
</UserControl>
